{
  "name": "@repo/api-contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "tsc -p tsconfig.scripts.json --noEmit && bun run scripts/generate-contract.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@orpc/contract": "^1.14.3"
  },
  "devDependencies": {
    "@app/api": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
