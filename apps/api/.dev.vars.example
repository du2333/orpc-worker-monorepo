# Optional local override for the non-secret example var in wrangler.jsonc.
EXAMPLE_GREETING_SUFFIX=from local .dev.vars

# Add API-only local secrets here later.
# This scaffold intentionally does not choose auth or database providers yet.
